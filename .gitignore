# ── Copyrighted client material — DO NOT commit (publisher IP + too large for GitHub) ──
# (the APK, extracted assets, libil2cpp.so, global-metadata.dat, decompiled dump)
*.apk
apk/
analysis/dump/
analysis/*.out.txt
captures/

# ── Third-party tools (download separately, see ROADMAP) ──
tools/Il2CppDumper/
tools/platform-tools/
tools/*.zip

# ── Generated TLS certs + PRIVATE KEYS (reproduce with server/gen_certs.py) ──
server/certs/

# ── Python / Node ──
__pycache__/
*.pyc
.venv/
venv/
node_modules/

# ── OS / editor ──
Thumbs.db
.DS_Store
.idea/
.vscode/
